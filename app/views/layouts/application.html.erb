<!DOCTYPE html>
<html>
<head>
  <title>Ten Thousand Hours</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <% if user_signed_in? %>
    <div align="right">Welcome, <%= current_user.email %>! | <%= link_to "Sign Me Out", destroy_user_session_path, method: :delete %></div>
    <% else %>
    <div align="right"><%= link_to "Sign In", new_user_session_path %> | <%= link_to "New User", new_user_registration_path %></div>
    <% end %>

    <% if notice %>
    <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
    <p class="alert"><%= alert %></p>
      <% end %>
<%= yield %>

</body>
</html>
